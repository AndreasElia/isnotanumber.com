{
  "name": "isnotanumber.com",
  "version": "1.0.0",
  "description": "Not a Number",
  "scripts": {
    "dev": "npx tailwindcss build -o ./public/tailwind.css",
    "build": "cross-env NODE_ENV=production npx tailwindcss build -o ./public/tailwind.css"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
